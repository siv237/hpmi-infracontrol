# deps
node_modules/

# private runtime data: AES key + stored servers with credentials
data/

# локальная страница wiki с тестовыми учётками (пароли не публикуем)
wiki/knowledge/test-accounts.md

# служебная информация сервера деплоя (имена/адреса/внутренние параметры)
wiki/knowledge/server-deploy.md

# runtime screenshots
screenshots/

# OS noise
.DS_Store
Thumbs.db
*.log

# reference binaries (GPL viewer from the iRMC web ui) — too big for git
raw/*.jar
avr_irmc_s2*.jar
*.jar

# env
.env

# офлайн-бандл деплоя (код + node_modules + runtime-ресурсы)
infracontrol-bundle.tgz

# кэш загруженных заголовков Node для офлайн-сборки native-модулей
.cache/
